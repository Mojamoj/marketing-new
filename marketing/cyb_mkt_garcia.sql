CREATE TABLE datas (
  id INT PRIMARY KEY AUTO_INCREMENT,
  full_name VARCHAR(255),
  email VARCHAR(255),
  product VARCHAR(255),
  payment VARCHAR(255),
  paying DATE,
  street VARCHAR(255),
  postal VARCHAR(255),
  region VARCHAR(255)
);
